import DataCollector from "../server/DataCollector.js";

describe("unit testing for the DataCollector class", () => {
    var collector;
    var obj;

    beforeEach(() => {
        collector = new DataCollector();

        obj = {

        }
    });

    test("", () => {
        
    });
});