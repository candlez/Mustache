import QuadTree from "../a/model/QuadTree.js";


describe('unit testing for QuadTree', () => {
    var tree;

    beforeEach(() => {
        tree = new QuadTree(128, 128, 256);
    })

    test('little test', () => {
        
    })
})